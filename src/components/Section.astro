---
const { title, class: className, id } = Astro.props;
---
<section id={id} class={`w-full mx-auto lg:w-[700px] ${className}`}>
    {title && <h2 class="text-4xl font-bold text-center md:text-left text-blue-500">{title}</h2>}
    <slot />
</section>